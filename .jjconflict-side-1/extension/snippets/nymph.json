{
	"Immutable binding": {
		"prefix": "let",
		"body": ["let ${1:name} = ${2:value}"],
		"description": "Bind an immutable value"
	},
	"Immutable state loop": {
		"prefix": "loop-state",
		"body": [
			"loop (let ${1:state} = ${2:initial}) {",
			"\tif (${3:done}) { break ${1:state} }",
			"\tcontinue(${1:state} = ${4:next})",
			"}"
		],
		"description": "Evolve immutable state through a named loop destination"
	},
	"Executable main": {
		"prefix": "main",
		"body": ["func main(): void = {", "\t$0", "}"],
		"description": "Create a valid executable semantic root"
	}
}
